# Dashboard Placeholder

Questa cartella e riservata alla futura dashboard Next.js.

## Direzione consigliata

- `app/(auth)` per login e magic link
- `app/documents` per lista e dettaglio documenti
- `app/reminders` per stato promemoria
- `lib/api.ts` per chiamate al backend FastAPI

Per l'MVP corrente non contiene codice eseguibile: serve come punto di aggancio per la prossima iterazione.

